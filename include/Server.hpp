#ifndef Server_HPP
#define Server_HPP

#include "Libs.hpp"

class Server
{
    public:
        // Constructors
        Server(void);

        // Copy constructor
        Server(Server const &src);

        // Destructor
        ~Server(void);

        // Copy assignment operator
        Server &operator=(Server const &rhs);

        //methods:        


    private:
    
};

#endif

