#ifndef SERVER_HPP
#define SERVER_HPP

#include "Libs.hpp"

class Server
{
    public:
        // Constructors
        Server(void);

        // Copy constructor
        Server(Server const &src);

        // Destructor
        ~Server(void);

        // Copy assignment operator
        Server &operator=(Server const &rhs);

        //methods:


    private:
            //data:


};

#endif
